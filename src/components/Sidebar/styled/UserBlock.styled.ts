import styled from 'styled-components';


const UserBlockStyled = styled.div`
  & svg {
    height: 2em;
    width: 2em;
  }

  & svg:last-child {
    margin-left: auto;
    margin-right: 0.2em;
    height: 1.2em;
    width: 1.2em;
  }
`

export default UserBlockStyled;
