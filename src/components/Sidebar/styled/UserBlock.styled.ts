import styled from 'styled-components';


const UserBlockStyled = styled.div`
  & svg {
    height: 2em;
    width: 2em;
  }

  & svg:last-child {
    height: 1.5em;
    width: 1.5em;
  }
`

export default UserBlockStyled;
