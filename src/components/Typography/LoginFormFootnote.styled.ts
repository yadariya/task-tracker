import styled from 'styled-components';

const LoginFormFootnote = styled.div`
  color: rgba(0, 0, 0, 0.5);
  font-size: 0.7em;
  text-align: center;

  & a {
    color: rgba(0, 0, 0, 0.5);
  }
  & p {
    margin: 0.5em 0;
  }
`;

export default LoginFormFootnote;
